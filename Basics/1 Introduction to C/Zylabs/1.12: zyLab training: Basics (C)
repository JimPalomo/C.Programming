/*
      While the zyLab platform can be used without training, a bit of training may help some students avoid common issues.

      The assignment is to get an integer from input, and output that integer squared, ending with newline.
*/

#include <stdio.h>

int main(void) {
   int userNum;
   int userNumSquared;
   
   scanf("%d", &userNum);
   
   userNumSquared = userNum * userNum;   // Bug here; fix it when instructed -- Fixed
   
   printf("%d\n", userNumSquared);         // Output formatting issue here; fix it when instructed -- Fixed

   return 0;
}

